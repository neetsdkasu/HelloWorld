#include <iostream>

using namespace std;

int main() {
    // FIRST
    // FIRST
    // FIRST
    // FIRST
    // FIRST
	for (int i = 1; i <= 15; i++) {
		if (i % 15 == 0) {
			cout << "FizzBuzz" << endl;
		} else if (i % 5 == 0) {
			cout << "Buzz" << endl;
		} else if (i % 3 == 0) {
			cout << "Fizz" << endl;
		} else {
			cout << i << endl;
		}
	}
    // SECOND
    // SECOND
    // SECOND
    // SECOND
    // SECOND
	// for (int x = 1; x <= 15; x++) {
		// if (x % 15 == 0) {
			// cout << "FIZZBUZZ" << endl;
		// } else if (x % 5 == 0) {
			// cout << "BUZZ" << endl;
		// } else if (x % 3 == 0) {
			// cout << "FIZZ" << endl;
		// } else {
			// cout << x << endl;
		// }
	// }
    // THRID
    // THRID
    // THRID
    // THRID
    // THRID
	for (int p = 1; p <= 15; p++) {
		if (p % 15 == 0) {
			cout << "fizzbuzz" << endl;
		} else if (p % 5 == 0) {
			cout << "buzz" << endl;
		} else if (p % 3 == 0) {
			cout << "fizz" << endl;
		} else {
			cout << p << endl;
		}
	}
    // 4th
    // 4th
    // 4th
    // 4th
    // 4th
	for (int i = 1; i <= 15; i++) {
		if (i % 15 == 0) {
			cout << "FizzBuzz" << endl;
		} else if (i % 5 == 0) {
			cout << "Buzz" << endl;
		} else if (i % 3 == 0) {
			cout << "Fizz" << endl;
		} else {
			cout << i << endl;
		}
	}
	return 0;
}

